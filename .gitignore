# HA runtime
.homekit.state
.storage/
deps/
home-assistant_v2.db*
*.log
*.db-shm
*.db-wal
.cloud/
.cloudbackup/
.media/
tmp/
tts/
.custom_components/__pycache__/
__pycache__/

# Secrets
secrets.yaml
*.key
*.pem

# Backups / uploads
backup/
uploads/

# OS/Editor
.DS_Store
.vscode/
.idea/

# HA add-on exports (beholdes i Git)
# (la denne MAPPEN bli med i Git – den er ment å trackes)
# Ingen ignore på addons_state/ – den SKAL med!

# Kilde for lokale add-ons (kopi for Git)
# Denne MAPPEN skal med i Git (ikke ignore)
# addons_local/

# HA add-on exports (beholdes i Git)
# (la denne MAPPEN bli med i Git – den er ment å trackes)
# Ingen ignore på addons_state/ – den SKAL med!

# Kilde for lokale add-ons (kopi for Git)
# Denne MAPPEN skal med i Git (ikke ignore)
# addons_local/
